{% from "govuk/components/table/macro.njk" import govukTable %}

{% macro overseasSitesTable(params) %}
  {{ govukTable({
    firstCellIsHeader: false,
    caption: params.caption,
    captionClasses: params.captionClasses,
    head: params.head,
    rows: params.rows
  }) }}
{% endmacro %}
