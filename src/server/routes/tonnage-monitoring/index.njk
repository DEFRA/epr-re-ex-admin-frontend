{% extends 'layout.njk' %}
{% from "govuk/components/table/macro.njk" import govukTable %}
{% from "govuk/components/inset-text/macro.njk" import govukInsetText %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <div class="govuk-body" data-testid="app-page-body">

        <h1 class="govuk-heading-xl">{{ pageTitle }}</h1>

        <p class="govuk-body">
          Data generated at: <strong>{{ generatedAt }}</strong>
        </p>

        {% if materials and materials.length %}
          {% set tableRows = [] %}
          {% for item in materials %}
            {% set materialCell = { text: item.material } %}
            {% set tonnageCell = { text: item.totalTonnage, format: "numeric" } %}
            {% set row = [ materialCell, tonnageCell ] %}
            {% set tableRows = tableRows.concat([row]) %}
          {% endfor %}

          {% set totalRow = [
            { text: "Total", classes: "govuk-!-font-weight-bold" },
            { text: total, format: "numeric", classes: "govuk-!-font-weight-bold" }
          ] %}
          {% set tableRows = tableRows.concat([totalRow]) %}

          {{ govukTable({
            captionClasses: "govuk-table__caption--m",
            firstCellIsHeader: true,
            head: [
              { text: "Material" },
              { text: "Tonnage", format: "numeric" }
            ],
            rows: tableRows
          }) }}

        {% else %}
          {{ govukInsetText({ text: "No tonnage data available." }) }}
        {% endif %}

      </div>
    </div>
  </div>
{% endblock %}
