{% extends 'layout.njk' %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <div class="govuk-body" data-testid="app-page-body">
        <h1 class="govuk-heading-xl">{{ heading }}</h1>
        <p>{{ message }}</p>
        <p>
          <a href="{{ uploadUrl }}" class="govuk-link">Try again</a>
        </p>
      </div>
    </div>
  </div>
{% endblock %}
